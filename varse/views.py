from django.http import HttpResponse

def home(request):
    return HttpResponse("Welcome to the Varse Vendor API — Backend is running successfully 🚀")
from django.http import HttpResponse

def home(request):
    return HttpResponse("Welcome to the Varse Vendor API — Backend is running successfully 🚀")
